# Fedja JSON

de.fedjapost.json


## Description

Fedja JSON is a minimal JSON implementation as API for Java (8).
Its purpose is to provide an easy way to deserialize, mutate and serialize JSON.
The API can be deployed for basically any purpose, is for JSON based configuration files or network payloads, without bloating application size.
